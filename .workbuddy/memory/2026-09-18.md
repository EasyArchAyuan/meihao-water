# 2026-09-18

## GEO 软文定时任务（自动化 74ec7dd5，首次无人值守完整跑通）
- 选题：按支柱配比缺口取 P3 → `idea-04`（廊坊公司订水避坑）。
- 产出 `content/news/company-water-order-checklist-langfang.md`，标题《廊坊公司订水避坑：行政管送水前该问清的三个问题》，汉字 1199 / 非空白 1502 / 内链 4（/langfang /products /shuineighbor /faq）。
- 门禁 `check:content` PASS（3 篇）；`next build` + `verify:ssg` 全 PASS（18 项，资讯页 3 篇）。
- commit `85e9bb0` `docs(content): …`，push `fb3ec70..85e9bb0`；CI/CD 04:05Z 起、04:06Z success；服务器 cron 10 分钟拉取。
- `content/ideas.json`：idea-04 `used=true` + `publishedSlug`。
- 精简版（300 字内 + 5 标签）追加到资料库文档 `FEhWWjYOY8Gqcp7x1thsAb` 末尾，新增「十二、本期 GEO 长文 · 头条号/小红书精简版」共 15 块，直接编辑即时落正文。

## 本轮新踩的坑（已写入 MEMORY.md）
- **PowerShell 里 `git push` 无论如何都 `cannot spawn sh`**（补 Git 的 `usr/bin` 到 PATH 也无效）→ push 必须用 Bash 工具。
- 中文 commit message 用 `git commit -F <UTF-8 无 BOM 文件>`。
- 门禁字数按「非空白字符」，用户口径是「汉字」；初稿 1365 汉字超标，需回炉收紧到 ≤1200。
