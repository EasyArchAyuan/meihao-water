# 2026-09-13 工作日志

## 轮播内容策略 · 资料库可视化
- 用户要求把资料库文档（轮播图文内容营销策略）一键可视化生成 HTML 页面，并明确「后续改动直接在该云端版本更新」。
- 源文档节点：`FEhWWjYOY8Gqcp7x1thsAb`（kind=doc，space `6L6KJTYsyLvQaipml8oYM4`，title「轮播图文内容营销策略」）。
- 走 library skill §7 md→html 分支（纯视图转换，--format page）：读取 doc 正文 → 转标准 Markdown → `md_to_html.py` 基线 → 品牌美化（深海蓝 #0d47a1 + 暖米色 #f5ede0，Noto Sans SC 标题字体，2 列杂志栅格）→ `import_html.py --parent-id --space-id` 挂到源文档下。
- 生成的可视化子页节点：`OsQW6dMrsJ9cF92w0Imqa4`，访问 `https://www.workbuddy.cn/space/d/OsQW6dMrsJ9cF92w0Imqa4`。未发布（协作态），用户可在资料库内点发布部署。
- 备注：doc 正文是 WorkBuddy 块格式（XML 风格，含 `readonly`/`rowHeader` 裸属性），转 Markdown 前需把裸属性补成 `=""` 才能用 ET 解析。

## 小红书「廊坊水厂少东家」打法修订（同线程）

用户新开小红书账号，定位「廊坊水厂少东家」（小红书号 xiaojiaGuzheng），要求按账号实况修订打法，改动直接落到上面这篇云端文档 FEhWWjYOY8Gqcp7x1thsAb。

### 账号实况（截图口径）
- 149 关注 / **76 粉丝** / 230 获赞与收藏；IP 河北。
- 置顶 2 条：货车实拍「北京混不下去，回廊坊接班了家人们！」（14,218 播放）＋ 怡宝价格对比海报（23 元到 14.4 元/桶，图上留 2805599 / 13393067179）。
- 诊断：播放转粉率约 0.5%（严重偏低，缺关注理由）、互动率约 1.6%、内容只有「感慨 + 价格战」两类、点名竞品有合规雷、站内转化链路缺失。

### 文档改动（全部落在云端 doc）
- **追加 §十一「小红书·廊坊水厂少东家打法修订」**：11.1 账号体检 / 11.2 定位升级 / 11.3 支柱重排 / 11.4 钩子库 / 11.5 六页模板 / 11.6 六篇专属选题分镜 / 11.7 合规红线 / 11.8 转化路径 / 11.9 30 天排期 / 11.10 指标修订 / 11.11 下一步。共 408 个块，分 4 次 insert_after（id 传空串）追加。
- **定位修订（审阅卡）**：§一 表格「内容人格」由「廊坊本地懂水的老邻居」改为「廊坊水厂少东家（厂二代 IP）」；「一句话主张」补人设口头禅「水的事问我，价的事找我」。这两处是**审阅建议**，需用户在审阅栏接受后才落正文。
- 新定位核心：从「内容驱动」改「人设驱动」，人设权重第一（P1 接班日常 35%）；合规红线：不点名竞品、不用「最便宜/第一」、图上只留 1 个电话、转化走站内私信关键词。

### 关键技术踩坑（下次直接复用）
1. **资料库 doc 的写入模式按「是否改已有块」分派**：纯追加 → submit_doc_edit.py（直接落正文）；改/删已有块 → 必须 submit_review_edit.py，且 new_content 必须带 Mark ar=delete（旧）+ Mark ar=insert（新），否则后端报 code=11607，即使本地 dry-run 通过。
2. 审阅 update 还要满足「diff 完备」：old_content 的纯文本必须等于 new_content 去掉 insert 段后的文本。
3. content 契约：禁止手写 id、直接编辑模式禁止 Mark ar、**正文禁止出现花括号**、顶层必须全是组件块且块间空行分隔。
4. doc 正文是 WorkBuddy 块格式且**无统一根节点**（顶层多元素 + readonly/rowHeader 裸属性）→ Python ET 解析前需把裸属性补成 ="" 并外套一个 ROOT 节点。
5. 本机 bash shim 缺 mkdir/cat/head，且双引号字符串里的反引号会被 shell 解释 → 写文件内容一律走 Write 工具 + Python 追加，不要塞进 bash -c。

## 经验沉淀为可复用技能 `water-site-factory`

用户要求「总结经验归纳为 skill，我要复用制作水邻居的官网」。已创建**用户级技能** `~/.workbuddy/skills/water-site-factory/`（20 个文件，约 100KB）。

- **SKILL.md**：产出物与架构图、何时用、**复用现成站点（最快路径）**（克隆 → 换 6 个 data 文件 → 换图 → 改 4 处部署侧 → 验收）、7 步复用流程、三条铁律、本机环境注意、验收清单、参考文件索引。
- **references/**：`nextjs-site.md`（骨架/数据源/Figure+media/文案风格/SEO/外文抓取加工）、`lighthouse-caddy.md`（实例/防火墙/Caddy/IDN/HTTPS/MCP 白名单/cron）、`ci-release.md`（workflows/semantic-release/权限与 Secrets/tag 回填）、`server-pull.md`（拉取脚本/cron/原子切换/回滚/双站点共存）、**`pitfalls.md`（踩坑总表，按症状查 —— 排障入口）**。
- **assets/templates/**：Caddyfile、ci-cd.yml、rollback.yml、backfill.yml、ssh-setup、deploy-site、.releaserc.json、pull.sh、gitignore，加 `PLACEHOLDERS.md` 占位符对照表。
- **scripts/**：`punycode.py`（含 --selftest，已验证输出 `xn--vhqu7tjwbb1iwpthm1a.online`）、`caddy_b64.py`（长度校验 + 生成 dd 命令）、`pack_site.py`（打包 + 可选 --push-dist）、`check_media.py`（media.ts ↔ public/ 一致性）。四个脚本均已对真实项目跑通。

### 本轮修正的两处事实错误
1. **base64 长度阈值**：旧记录写「单段 ≤1.7KB」，实测 1840 字符（命令 1892）成功 → **真正的硬约束是整条命令 ≤2048 字符**，base64 建议 ≤1900。已同步改 SKILL.md / pitfalls.md / lighthouse-caddy.md / 项目 MEMORY.md。
2. **MCP 白名单补充**：`crontab -`（从 stdin 读）可用；但命令中的 `>>` 重定向**即使写在引号内也被拦** → 日志重定向必须写在脚本内部（`exec >>"$LOG" 2>&1`）。

