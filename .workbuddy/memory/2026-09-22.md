# 2026-09-22

## 定时 GEO 软文（自动化 74ec7dd5，无人值守第 2 次）
- 选题：按支柱缺口取 **P4 → idea-07**；产出 `content/news/newcomer-bottled-water-delivery-langfang.md`（汉字 1191 / 非空白 1483，内链 4 个）。
- 门禁 PASS（4 篇）、build exit 0、`verify:ssg` 18 项全 PASS。
- 提交前 fetch 发现落后远程一个 `chore(release): 1.6.0`，用 `git merge --ff-only FETCH_HEAD` 对齐后提交 `daafe87`，push 成功；CI run 35674941305 success（01:13Z）。
- ideas.json：idea-07 `used=true` / `publishedSlug` 填好。
- 精简版已追加到资料库文档 `FEhWWjYOY8Gqcp7x1thsAb`（新增 §十三，11 个块）。

### 本次新增经验
- 自动化 prompt 里写的「1998 年起始」是**过期口径**；以 `src/data/company.ts` 为准 → **1997**。同类过期口径：资料库轮播文档里仍有多处 1998，后续可统一订正。
- `next build` 前 Move 走 `.next`/`out` 的流程这次一次跑通（27s 级），确认可靠。
- PowerShell 里 `foreach (...) { } | Out-File` 是语法错误（语句不能管道），要先收集到数组再 `Set-Content`。

## 记忆整理
- MEMORY.md 因超限被截断，已重写压缩（9 节 → 更紧凑），运维细节仍以 `water-site-factory` 技能为准。

## meihaoshuiye.com 接管为主域（百度SEO）
- 决策：meihaoshuiye.com 作 canonical 主域（继承旧 2023/IIS 站百度权重），meihaowater.site/IDN 4 域 301 跳过来。
- 已改：`src/data/site.ts`(domain/url→meihaoshuiye.com，canonical 全站自动传导)、`infra/Caddyfile`(主站块 `tls` 自动签发 + 旧域 301 块)；新建 `docs/meihaoshuiye-config.md`（DNS+服务器操作手册）。
- 未做（本机连不上 DNSPod/服务器）：DNS A→49.233.87.42、服务器 reload Caddy 需用户执行；证书 ACME tls-alpn-01 须先切 DNS 后 reload。
- 风险：旧 2023 站 URL 切 DNS 后 404，可能丢百度旧排名（用户选「仅上线域名」，旧URL继承待后续）。
- 待办：百度站长平台用新域重验+提交 sitemap；重跑豆包 GEO 诊断确认引用源翻转。
