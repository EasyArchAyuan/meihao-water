# 2026-09-10

## 美好水业官网 2.0 — Phase 1（设计系统 + 首页信息架构）
- 依据 `美好水业官网 2.0 完整开发 Prompt.md` 输出 Phase 1 文档：`docs/phase1-design-system.md`（A–K 全量：IA、构图、文案、色/字/间距/响应式/动效系统、组件树、图片清单、SEO 方案）。
- 关键决策：
  - 首页 11 个 Section 单列叙事；深色仅 3 处（02 二十年 / 06 一次性桶装水 / Footer）。
  - 色彩：主背景 #F7F8F5、辅助 #EEF3F1、深色 #0B1F2A、主色深海蓝 #0B3C4E、水邻居蓝绿 #63C9C1（浅底文字用 #17786F）。
  - 字号全 clamp()：Hero clamp(42,7.4vw,96)、Section clamp(36,5.2vw,72)、正文 clamp(16,1.05vw,18)。
  - 间距 section-y clamp(88,11vw,200)，容器 1240，阅读列 640。
  - 动效只允许 fade/translateY/scale≤1.04/轻微 parallax/navbar blur/hover；统一 `<Reveal>` + prefers-reduced-motion。
  - 20 年时间轴**不标年份**（避免虚构事实），改用关键词节奏；成立年份留 TODO。
  - 图片统一 `data/media.ts` + `<Figure>` 组件，未拿到实拍时渲染占位并标记 `TODO: REPLACE_WITH_REAL_IMAGE`。
  - 数据层 `data/site.ts` / `company.ts` / `products.ts`；未知信息一律 TODO，禁止编造电话/地址/资质/数据。
- 流程约定：Phase 1 已暂停等确认，确认后进入 Phase 2（Next.js 16 + TS + Tailwind 4 + shadcn + Motion + Lucide 搭建）。
- 待用户确认：真实电话/地址/成立年份/抖音号/ICP；是否有实拍图；「立即订水」一期入口形式；水邻居是否独立域名。

## 美好水业官网 2.0 — Phase 2–7（实现完成）
- 用户 6 条确认：1998 成立（→28 年文案"二十余年/自 1998 年起"）、3 个真实订水电话、北凤道 399 地址、代理品牌 10 个、CTA 透出电话+二维码、水邻居独立域名后续跳转。logo.jpg 已提供。
- 关键决策（写入 phase1 附录）：
  - 色彩 token 修正 3 处：`--brand` `#0B3C4E` → `#0D47A1`（取自 logo 下半深蓝），新增 `--brand-bright` `#1E88E5`（取自 logo 上半亮蓝），`--brand-deep` `#0B1F2A` → `#0A2540`，`--ink` `#12181C` → `#0A1A2A`。
  - Logo 渐变豁免：logo.jpg 本身保留亮蓝→深蓝渐变 + 书法字；深色 Section 用自制的 `public/brand/logo-white.svg`。
  - 新增 `/brands` 页 + `/products` 底 strip + 首页 06 文末文字提及（**不下载任何品牌 logo 图**）。
  - 02 Section 改"二十余年"，不标 1998/2026 具体年份节点。
- 落地：
  - Next.js 16.3.4 + React 19.2.8 + Tailwind 4 + motion 12 + lucide-react 0.460 + TypeScript 5；npm 10.9.7。
  - 14 路由全部静态生成（/、/about、/products、/shuineighbor、/contact、/brands、/service、/icon.svg、/opengraph-image、/robots.txt、/sitemap.xml、/_not-found）。
  - `npm run lint` 0 错误 0 警告；`npm run build` 成功；`next start -p 3030` 起，9 路由全部 200。
- 踩坑（详见 docs/phase2-implementation.md §3）：
  1. `create-next-app` 拒绝中文目录 → 临时目录 mhsy-temp 初始化再 cp 回去。
  2. Git Bash 下 `.bin/next` sh 包装路径转换失败 → 改用 `node ./node_modules/next/dist/bin/next`。
  3. `as const` 推断 readonly 与 Metadata.keywords 类型不匹配 → `as string[]` 显式标注。
  4. React 19 `react-hooks/set-state-in-effect` lint → 改用"渲染期间根据 prop 变化重置 state"模式。
  5. `no-img-element` 警告 → Logo 用 `<img>` + `eslint-disable-next-line` 注明意图。
  6. ButtonLink 解构未使用变量 → 重构 props 解构 + 提取 buildClass。
- 当前状态：MVP 完成；已知 TODO 见 phase2-implementation.md §6（真实图片 9 张 + 微信二维码 + ICP + 水邻居域名 + 产品规格 4 项）。
- 下一步：等用户提供真实业务图片 + 微信服务号二维码，替换 `data/media.ts` 中 `src: null` 即可。

## v1.0.0 发布
- 2026-09-10 18:34 完成首个正式发布
- package.json 版本：`0.1.0` → `1.0.0`
- 新增 `CHANGELOG.md`（Keep a Changelog 1.1 格式，简体中文）
- `.gitignore` 扩展：保留 `.workbuddy/memory/` 与 `.workbuddy/skills/`，忽略 OS/IDE/日志
- 初始化 git 仓库（`main` 分支），单次提交 57 个文件
- 打 `v1.0.0` annotated tag
- 提交信息遵循 Conventional Commits：`chore(release): v1.0.0 — 美好水业官网 2.0 首发`
