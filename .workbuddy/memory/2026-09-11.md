# 2026-09-11

## 移动端布局专项 → 发布 v1.0.1

任务：「调整移动端布局，让手机端信息展示更合理」。

### 改动范围
23 个文件（源码）+ 收尾 7 个文件。涉及 7 个页面、10 个首页 Section。

### 核心修复（含 1 个本轮新引入的回归）
1. **结尾 CTA 二维码宽度回归**（本轮新引入）：把 `w-32 sm:w-36` 写进了 `Figure` 的 `className`，但 `Figure` 基类含 `w-full`，且 `cn()` 不做冲突消解 → 实测编译 CSS `.w-32`@84821 < `.w-full`@84897，同层同优先级后者胜出 → 手机上二维码被撑成整列宽。修复：宽度交由外层 `<div>` 约束。
   **经验**：本项目 `Figure` 的 `className` 不可用于控制宽度，必须用外层容器。
2. **二十年 Section 横向溢出 145px**（P0）：巨字 `clamp(120px,18vw,260px)` 下限过大，375px 屏 4 字 = 480px > 可用 335px。改为 `clamp(60px,15.5vw,220px)`。
3. **14 处标题嵌套** `<h1><h1>` / `<h2><h2>`：为 `RevealItem` 增加 `id` 透传，标题改单层。
4. **WhatWeDo 移动端描述恒为第 1 项**：原依赖 `onMouseEnter` 更新 `active`，触摸无 hover。改为移动端每行始终渲染自身描述。
5. **Logo 白色版在深底不可见**：SVG 经 `<img>` 加载时 `currentColor` 解析为黑（不继承父级颜色）；且 220:64 的 viewBox 被按 40×40 渲染而压扁。→ 硬编码白色 + 按 220:64 算宽度 + 去掉重复品牌名。
6. **Footer 移动端网格错位**：3 栏进 `grid-cols-2` → 移动端改为「网站 + 联系」两列，订水电话独立为大字号优先区块。
7. **配送流程移动端竖线断开** → 绝对定位竖线贯穿。

### 变更（信息层级）
- Hero 大图移动端 4/5（原 16/9 仅约 190px 高）；`Figure` 新增 `ratioSm`
- `Figure` 占位改容器查询 `@container` + `@max-[200px]`，小容器只留单行
- Navbar 移动端新增一键拨号；ClosingCTA 三入口改带分隔线清单
- 二十年三图：`row-span-2` 拼贴 → 3 列等宽
- CitySection 城市线条移动端隐藏（穿过正文 + slice 放大变粗），加 `vector-effect: non-scaling-stroke`
- 触控目标统一 ≥44px
- 多处移动端间距降级，修正重复断点

### 移除
- `src/components/ui/SectionHeading.tsx`（零引用）
- `Figure` 中永不生效的 `?? "aspect-[16/9]"`

### 验证
- `npm run lint` 0 error 0 warning；`npm run build` 14 路由静态生成
- 7 页 `h1=1, nested=0`（编译后 HTML）
- 320/375/390/414/768 × 7 页零横向滚动（真实 Chrome via CDP）
- 触控目标仅剩 skip-link（sr-only 豁免）
- 编译 CSS 含 `\@container{container-type:inline-size}` + `@container not (min-width:200px){…}`
- ⚠️ 收尾后浏览器复验受限：headless Chrome 在本会话秒退无输出 → 改用编译产物 + HTML 静态校验。收尾改动均为宽度中性/收窄，不影响既有结论。

### 环境坑（重要，下次直接复用）
本会话环境被清空 `PATH` 与 `SystemRoot`，导致 `ls`/`dirname`/`tail` 不可用、Chrome 秒退。恢复：
```bash
export PATH="/usr/bin:/bin:/c/Windows/System32:/c/Windows"
export SystemRoot="C:\\Windows"
```
- node 实际路径：`C:\Users\shang\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`（**不是** 22.22.2-2）
- Git Bash 下 `.bin/next` 是 sh 包装，路径转换会失败 → 用 `node ./node_modules/next/dist/bin/next <cmd>`
- 中文目录名会被 `create-next-app` 拒绝 → 先在英文临时目录初始化再复制

### 辅助脚本（临时目录，未入仓库）
`C:\Users\shang\AppData\Local\Temp\mhsy-check\`：
- `overflow-check.mjs` — 逐页探测横向溢出（`scrollWidth vs clientWidth`）
- `tap-check.mjs` — 探测 <44px 的 a/button
- `shot.mjs` — 分段整页截图（注入 CSS 禁用动画）
均基于 CDP（headless Chrome `--remote-debugging-port=9222`）。

### 发布
- `package.json` → `1.0.1`
- CHANGELOG 新增 `[1.0.1] — 2026-09-11`；修正 1.0.0 段中已过期的 clamp 值描述
- `docs/phase2-implementation.md` 新增 §10 移动端专项（含验证方法与结论）
- `docs/phase1-design-system.md` 新增「附录 2（v1.0.1 修订）」6 条；修正 logo-white 的 currentColor 说明
- git commit + `v1.0.1` tag

### 遗留 TODO（未变）
真实图片 11 张 + 微信二维码；ICP / 抖音号 / 水邻居域名；4 类产品规格；`/service` 实装；Lighthouse 报告。

---

## 追加：v1.0.2 移动端菜单字号下调

用户反馈「字体太大了」（附移动端全屏菜单截图）。

**改动**（`src/components/layout/MobileMenu.tsx`）：
- 导航项 `clamp(36px,9vw,56px)` → `clamp(26px,7vw,38px)`（375px 实际 36→26px）
- 菜单内电话 `clamp(32px,8vw,44px)` → `clamp(24px,6.2vw,32px)`
- `gap-2 → gap-1`、`py-5 → py-3.5`、`pt-24 pb-10 → pt-20 pb-8`

**排查结论（重要经验）**：截图中菜单链接显示为浏览器默认蓝色，但**不是本项目的 bug**。
- 已核查：`--ink`/`--ink-soft` 变量存在；`.text-\[var\(--ink\)\]{color:var(--ink)}` 已生成；`<a>` 仅有 preflight 的 `a{color:inherit}`（在 `@layer base`，被 utilities 覆盖）。
- 决定性证据：**同一张截图里** `美好水业`（`<span>`，同一个 `text-[var(--ink)]` 类）是深色正常，`2805599`（`<a>`，同一个类）是蓝色 → 类生效，只有 `<a>` 被外部覆盖。
- 最可能原因：浏览器扩展注入**未分层**的 `a{color:…}`。CSS 级联层中未分层声明优先级 > `@layer` 内声明，故压过 Tailwind utilities。
- 结论：建议用户在无痕窗口复核；**不要**为此加 `!important` 或未分层的 `a{color:inherit}`（后者反而会破坏所有链接配色）。

**验证**：lint 0 error；清空 `.next` 后 build 14 路由；编译产物确认新 clamp 存在、旧 clamp 已移除。

### 附：排查用脚本
`C:\Users\shang\AppData\Local\Temp\mhsy-check\css-check.mjs`（变量/工具类/`a` 规则审计）、`font-check.mjs`（字号替换确认）。
**注意**：不要用「读 `.next` 全部 CSS」的方式判断构建产物 —— 旧 chunk 会残留导致误判，需先 `rm -rf .next` 再 build。同时 CSS 压缩会去掉逗号后空格，探测字符串要写成 `clamp(26px,7vw,38px)` 而非 `clamp(26px, 7vw, 38px)`。
